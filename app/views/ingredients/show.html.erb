<ul class="nav nav-pills">
  <li><%= link_to('New Ingredient', new_ingredient_path) %></li>
  <li><%= link_to('Edit Ingredients', edit_ingredient_path(@ingredient)) %></li>
  <li><%= link_to('Delete Recipe', @ingredient, :method => :delete, :confirm => 'Are you sure?') %></li>
  <div class='spacer'></div>
</ul>

<div class="row">
  <div class="col-md-5 col-sm-5">
    <p class="lead"><%= @ingredient.name %></p>
    <p><%= @ingredient.name %>
    <p><%= @ingredient.brand %>
  </div>

  <!-- TODO: Display the ingredients in the show page -->
  <!-- <div class="col-md-7 col-sm-7">
    < @recipe.ingredients.each do |ingredient| %>
      < ingredient.name %>
      < image_tag(ingredient.image,  size: "40x40") %>
    < end %> -->
  </div>
 </div>